Ejemplo compilación: g++ main.cpp blocks.cpp blocks_parallel.cpp strassen.cpp utility.cpp -fopenmp -o m

El archivo donde se realizan los cálculos es main.cpp.

Para utilizar simplemente ejecutar ./m.
